<form method="POST" action="{{ route('programs.lessons.toggle', [$program, $lesson]) }}" class="inline">@csrf @method('PATCH')<input type="hidden" name="completed" value="0"><label class="inline-flex cursor-pointer items-center gap-2 text-sm {{ $lesson->completed_at ? 'text-emerald-700' : 'text-slate-600' }}"><input type="checkbox" name="completed" value="1" @checked($lesson->completed_at) onchange="this.form.submit()"><span>{{ $lesson->completed_at ? __('Completed') : __('Mark as completed') }}</span></label></form>
