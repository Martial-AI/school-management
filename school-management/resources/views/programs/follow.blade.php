<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl">{{ __('Program tracking') }}</h2></x-slot><div class="mx-auto max-w-4xl py-8 sm:px-6 lg:px-8"><div class="rounded-xl bg-white p-6 shadow"><div class="mb-5"><h3 class="text-lg font-semibold">{{ $program->title }}</h3><p class="text-sm text-slate-500">{{ $program->subject?->localizedName() }} · {{ $program->schoolClass?->name }} · {{ $program->period_name }}</p></div><div class="space-y-4">@forelse($program->lessons as $chapter)<section class="rounded-xl border border-slate-200 p-4"><div class="flex items-center justify-between"><h4 class="font-semibold">{{ $chapter->title }}</h4>@include('programs.partials.lesson-check',['lesson'=>$chapter,'program'=>$program])</div>@foreach($chapter->children as $lesson)<div class="mt-3 flex items-center justify-between rounded-lg bg-slate-50 px-3 py-2"><span class="text-sm">↳ {{ $lesson->title }}</span>@include('programs.partials.lesson-check',['lesson'=>$lesson,'program'=>$program])</div>@endforeach</section>@empty<div class="py-10 text-center text-slate-500">{{ __('Add chapters in Edit before starting tracking.') }}</div>@endforelse</div></div></div></x-app-layout>
