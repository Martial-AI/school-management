<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl">{{ __('Create a teaching program') }}</h2></x-slot><div class="mx-auto max-w-2xl py-8 sm:px-6 lg:px-8"><form method="POST" action="{{ route('programs.store') }}" class="space-y-3 rounded-xl bg-white p-6 shadow">@csrf
@if($isAdmin)<label class="block">{{ __('Teacher') }}<select name="teacher_id" required class="mt-1 w-full rounded">@foreach($teachers as $teacher)<option value="{{ $teacher->id }}">{{ $teacher->localizedFunctionLabel() }}</option>@endforeach</select></label>@else<input type="hidden" name="teacher_id" value="{{ auth()->id() }}">@endif
<label class="block">{{ __('Class') }}<select name="school_class_id" required class="mt-1 w-full rounded">@foreach($classes as $class)<option value="{{ $class->id }}">{{ $class->name }}</option>@endforeach</select></label><label class="block">{{ __('Subject') }}<select name="subject_name" required class="mt-1 w-full rounded"><option value="">{{ __('Choose a subject') }}</option>@foreach($subjects as $subject)<option value="{{ $subject->name }}" @selected(old('subject_name') === $subject->name)>{{ $subject->localizedName() }}</option>@endforeach</select></label><label class="block">{{ __('Program') }}<input name="title" placeholder="{{ __('Program') }}" required class="mt-1 w-full rounded"></label><label class="block">{{ __('Period') }}<input name="period_name" placeholder="{{ __('Period') }}" required class="mt-1 w-full rounded"></label><div class="grid grid-cols-2 gap-3"><label class="block">{{ __('Start') }}<input type="date" name="starts_on" required class="mt-1 w-full rounded"></label><label class="block">{{ __('End') }}<input type="date" name="ends_on" required class="mt-1 w-full rounded"></label></div><label class="block">{{ __('Objectives') }}<textarea name="objectives" class="mt-1 w-full rounded"></textarea></label><button class="rounded bg-emerald-700 px-4 py-2 text-white">{{ __('Save program') }}</button></form></div></x-app-layout>
